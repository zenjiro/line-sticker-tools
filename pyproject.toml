[project]
name = "line-sticker-tools"
version = "1.0.0"
description = "Simple tools for creating LINE stickers"
requires-python = ">=3.11"

dependencies = [
    "Pillow==10.4.0",
    "scipy==1.14.1", 
    "numpy==2.1.3",
]
