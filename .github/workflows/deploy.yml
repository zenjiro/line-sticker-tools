name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: write
  deployments: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Dependencies (sticker-gui)
        run: npm ci
        working-directory: sticker-gui

      - name: Build (sticker-gui)
        run: npm run build
        working-directory: sticker-gui

      - name: Install Dependencies (remove-bg-gui)
        run: npm ci
        working-directory: remove-bg-gui

      - name: Build (remove-bg-gui)
        run: npm run build
        working-directory: remove-bg-gui

      - name: Determine Target Folder
        id: target
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "folder=pr-preview/${{ github.event.number }}" >> $GITHUB_OUTPUT
          else
            echo "folder=." >> $GITHUB_OUTPUT
          fi

      - name: Prepare Combined Dist
        run: |
          mkdir -p combined-dist
          cp -r sticker-gui/dist/* combined-dist/
          mkdir -p combined-dist/remove-bg
          cp -r remove-bg-gui/dist/* combined-dist/remove-bg/

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: combined-dist
          target-folder: ${{ steps.target.outputs.folder }}
          clean: ${{ github.event_name != 'pull_request' }}
          clean-exclude: pr-preview/

      - name: Create Deployment Status
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const deployUrl = `https://${context.repo.owner}.github.io/${context.repo.repo}/pr-preview/${context.issue.number}/`;
            
            // Create deployment
            const deployment = await github.rest.repos.createDeployment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: context.payload.pull_request.head.sha,
              environment: 'pr-preview',
              auto_merge: false,
              required_contexts: [],
              transient_environment: true
            });
            
            // Create Deployment Status
            await github.rest.repos.createDeploymentStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              deployment_id: deployment.data.id,
              state: 'success',
              environment_url: deployUrl,
              log_url: `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              description: 'Preview is ready!'
            });
